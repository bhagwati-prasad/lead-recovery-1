version: '3.9'
services:
  backend:
    image: node:20
    working_dir: /app
    volumes:
      - ../backend:/app
    command: sh -c "npm install && npm run start:dev"
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: development
      APP_PORT: 3000
